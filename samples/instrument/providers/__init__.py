"""Provider-adapter sub-samples (M3 fan-out).

Each sub-package here demonstrates a single LLM provider adapter
end-to-end: SDK client → ``VertexAdapter`` (or sibling) →
``HttpEventSink`` → atlas-app.
"""

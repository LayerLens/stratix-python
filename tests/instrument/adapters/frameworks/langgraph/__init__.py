"""Dedicated tests for the LangGraph framework adapter.

Ported from ``ateam/tests/adapters/langgraph/`` so that stratix-python
matches ateam's per-module coverage instead of relying on a single
consolidated smoke test (``tests/instrument/adapters/frameworks/test_langgraph.py``).
"""

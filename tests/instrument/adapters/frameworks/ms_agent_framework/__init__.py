"""Dedicated tests for the ms_agent_framework framework adapter.

Ported from ``ateam/tests/adapters/ms_agent_framework/`` so that
stratix-python matches ateam's deeper coverage instead of relying
solely on the consolidated smoke test
(``tests/instrument/adapters/frameworks/test_ms_agent_framework_adapter.py``).
"""

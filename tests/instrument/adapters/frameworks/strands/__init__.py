"""Dedicated tests for the strands framework adapter.

Ported from ``ateam/tests/adapters/strands/`` so that
stratix-python matches ateam's deeper coverage instead of relying
solely on the consolidated smoke test
(``tests/instrument/adapters/frameworks/test_strands_adapter.py``).
"""

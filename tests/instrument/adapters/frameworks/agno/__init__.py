"""Dedicated tests for the Agno framework adapter.

Ported from ``ateam/tests/adapters/agno/`` so that stratix-python
matches ateam's per-module coverage instead of relying on a single
consolidated smoke test (``tests/instrument/adapters/frameworks/test_agno_adapter.py``).
"""

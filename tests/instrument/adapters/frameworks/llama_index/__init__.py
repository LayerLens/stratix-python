"""Dedicated tests for the LlamaIndex framework adapter.

Ported from ``ateam/tests/adapters/llama_index/`` so that stratix-python
matches ateam's per-module coverage instead of relying solely on the
single consolidated smoke test
(``tests/instrument/adapters/frameworks/test_llama_index_adapter.py``).
"""

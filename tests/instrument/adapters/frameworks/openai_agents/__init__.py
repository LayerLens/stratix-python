"""Dedicated tests for the OpenAI Agents framework adapter.

Ported from ``ateam/tests/adapters/openai_agents/`` so that
``stratix-python`` matches ateam's per-module coverage instead of relying
solely on the consolidated smoke test
(``tests/instrument/adapters/frameworks/test_openai_agents_adapter.py``).
"""
